import { LoaderContainer, LoaderSpinner } from './Loader.styled'

const Loader = () => {
    return (
        <LoaderContainer>
            <LoaderSpinner />
        </LoaderContainer>
    )
}

export default Loader
